# Eventmarket calendar

## To run the code

´´´ $ git clone ´´´
´´´ $ cd to local clone of git repo ´´´
´´´ $ bundle install ´´´
´´´ $ bundle exec middleman server ´´´

## Requirements

* Ruby
* Middleman gem (just bundle install and we'll fix this one for you. No extra cost!)